# راهنمای فضای کاری کلینیک مهرگان برای مدل بعدی

تاریخ تهیه: ۱۸ سپتامبر ۲۰۲۶

## ۱) مرز و منبع حقیقت

ریشهٔ معتبر پروژه همین مسیر است:

```text
Z:\Projects\mehreganClinic\Version5New
```

این پروژه یک برنامهٔ React/Vite + Express + PostgreSQL است. دامنهٔ تولید:

```text
https://mehreganpetclinic.ir
```

دادهٔ واقعی باید از PostgreSQL خوانده شود. فایل‌های `.env`، `uu.txt`، فایل‌های احراز هویت، توکن‌ها و دادهٔ بالینی محرمانه‌اند و نباید خوانده یا در گزارش چاپ شوند.

`AGENTS.md` دستورالعمل جاری و معتبر پروژه است. بعضی مسیرهای قدیمی داخل `HANDOFF_VERSION5_NEXT_AGENT.md` با مسیر فعلی مغایرت دارند؛ از مسیر فعلی و `AGENTS.md` پیروی کنید.

## ۲) معماری و نقاط ورود

- `server.ts`: سرور Express، API، احراز هویت، ذخیره‌سازی و اتصال PostgreSQL.
- `src/App.tsx`: پوستهٔ اصلی، نشست کاربر و انتخاب بخش‌ها.
- `src/components/`: پنجره‌ها و اجزای مشترک، دعوت کاربر، مدیر ارشد، جستجو و صدا.
- `src/components/tabs/`: جریان‌های کاری کلینیک مانند پرونده، پذیرش، نوبت، واکسیناسیون، جراحی، صندوق، پانسیون و پت‌شاپ.
- `src/services/apiClient.ts`: ارتباط frontend با API.
- `src/services/dataCache.ts`: کش سمت کلاینت.
- `src/services/voiceSecretary.ts`: دریافت فرمان صوتی و تبدیل آن به جستجو/فرمان.
- `src/types.ts`: قرارداد دادهٔ frontend.
- `scripts/productionSmoke.mjs`: آزمون سلامت API تولید.
- `scripts/checkStageSafety.mjs`: کنترل ایمنی اسناد RM.
- `scripts/validateRmStage.mjs`: اعتبارسنجی مرحله‌های RM-JSON.
- `seed/seed_clinic_master_data.json`: دادهٔ مرجع اولیه؛ برای ساخت دادهٔ نمایشی در مسیر تولید استفاده نشود.
- `config/roles.json`: نقش‌ها و مجوزهای پایه.
- `deploy/` و `deploy*.sh|ps1`: اسکریپت‌های استقرار؛ قبل از اجرا نسخهٔ rollback بسازید.

پوشه‌های `node_modules/` و `dist/` عمداً در این سند معرفی نشده‌اند؛ اولی وابستگی تولیدشده و دومی خروجی build است.

## ۳) وضعیت فعلی تثبیت‌شده

- اتصال PostgreSQL و API عملیاتی است؛ نام دیتابیس باید با حروف کوچک `clinic_db` باشد.
- پرونده‌ها و داده‌های واقعی مهاجرت‌شده در دیتابیس موجودند.
- داشبورد، نقش‌ها، ورود کاربران، دعوت کاربر و نقش قفل‌شده توسط مدیر/کارشناس IT پیاده‌سازی شده‌اند.
- شمارندهٔ «پرونده‌های موجود در سیستم» از حضور فعلی در کلینیک جدا شده است.
- جستجوی پرونده در سمت سرور محدود به ۱۰ نتیجهٔ مرتبط است و با ورود متن دقیق‌تر می‌شود.
- جستجوهای سمت کلاینت null-safe شده‌اند تا خطای `includes` روی مقدار تهی رخ ندهد.
- انتخاب پرونده در پروندهٔ پزشکی، نوبت، پذیرش فوری و پانسیون از `RemotePetSearchSelect` استفاده می‌کند.
- کش PWA و به‌روزرسانی service worker تنظیم شده است.
- بخش‌های ناقص/آزمایشی نباید برای کاربران عادی نمایش داده شوند؛ تکمیل هر بخش فقط پس از اتصال واقعی به API و آزمون مجاز است.
- commit پایهٔ محلی موجود است: `907dd05`.
- آرشیو rollback خارج از workspace ساخته شده است؛ مسیر و محتوای آن را در گزارش عمومی بازنشر نکنید مگر کاربر درخواست کند.

## ۴) قرارداد توسعه و آزمون

پس از هر تغییر کد، حداقل این‌ها را اجرا کنید:

```text
npm run lint
npm run build
```

برای تغییرات API یا داده:

```text
npm run production:smoke
```

برای اسناد برنامه‌ریزی:

```text
npm run rm:safety
node scripts/validateRmStage.mjs <stage-file>.json
```

هر نتیجه باید با شواهد کوتاه گزارش شود: نام آزمون، موفق/ناموفق، و blocker احتمالی. از چاپ env، رمز، توکن، دادهٔ بالینی خام و خروجی کامل دیتابیس خودداری کنید.

## ۵) هدف «تکامل الف»

تکامل الف یعنی نسخه‌ای که جریان اصلی کلینیک با دادهٔ واقعی، پایدار و قابل استقرار باشد: ورود و نقش، جستجوی سریع پرونده، ثبت ورود، پروندهٔ پزشکی، واکسیناسیون، نوبت، پرداخت/صورتحساب، پیام وظیفه و گزارش وضعیت. گزینه‌هایی که هنوز backend، مجوز، ذخیره‌سازی و آزمون واقعی ندارند باید hidden یا disabled بمانند؛ صرفاً ظاهر یک دکمه معیار تکمیل نیست.

## ۶) روش کار مدل بعدی

قبل از تغییر، ابتدا وضعیت و diff را بخوانید. تغییرات کاربر را حفظ کنید. کار را در گام‌های بزرگ انجام دهید، هر گام را با آزمون ببندید و فقط نتیجه، blocker و قدم بعد را گزارش کنید. برای دادهٔ واقعی ابتدا read-only check انجام دهید. هیچ رکورد نمایشی به مسیر تولید اضافه نکنید.

