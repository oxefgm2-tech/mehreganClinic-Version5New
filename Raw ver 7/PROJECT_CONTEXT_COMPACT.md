# زمینهٔ فشردهٔ ادامهٔ پروژهٔ کلینیک مهرگان

تاریخ ثبت: ۱۷ سپتامبر ۲۰۲۶

## وضعیت قطعی فعلی

- پروژه در همین مسیر فعال است: `Z:\Projects\mehreganClinic\Version5New`
- محصول React/Vite + Express است و PostgreSQL منبع دادهٔ واقعی آن است.
- نسخهٔ مستقر روی `mehreganpetclinic.ir` و VPS فعال است.
- داده‌های مهاجرت‌شده واقعی هستند؛ دادهٔ نمایشی نباید به کاربر نشان داده شود.
- کش کلاینت با IndexedDB و به‌صورت کاربرمحور استفاده می‌شود.
- fingerprint جداول PostgreSQL و بررسی پس‌زمینه برای تشخیص تغییرات فعال است.

## ملاحظات و بن‌بست‌های ثبت‌شده

1. مسیر درج‌شده در handoff با مسیر واقعی این workspace یکسان نبود؛ همیشه مسیر فعلی محیط را مبنا قرار بده.
2. نام دیتابیس PostgreSQL باید با حروف کوچک استفاده شود؛ تفاوت `Clinic_db` و `clinic_db` باعث خطای اتصال شد.
3. نمایش داشبورد پس از اتصال کامل دیتابیس کند و از نظر تجربهٔ کاربر نامناسب بود؛ راه‌حل: نمایش سریع کش معتبر و همگام‌سازی پس‌زمینه.
4. کش عمومیِ کل داده‌های درمانی روی کلاینت مناسب نبود؛ راه‌حل: کش IndexedDB با کلید کاربر واردشده.
5. تازه‌سازی مستقیم جداول نرمال‌شده می‌توانست تغییرات `clinic_store` را بازنویسی کند؛ اکنون این دو منبع در fingerprint تفکیک شده‌اند.
6. تست بدون شواهد کافی نباید به‌عنوان موفقیت اعلام شود؛ هر اقدام باید تست پذیرش مشخص داشته باشد.
7. credentialها، رمزها، توکن‌ها و دادهٔ خام پرونده نباید در گزارش، خروجی ابزار یا artifact فرایندی چاپ شوند.
8. گزارش‌های طولانی و بازخوانی کامل فایل‌ها مصرف توکن را بالا می‌برد؛ خروجی باید محدود، هدفمند و مرحله‌ای باشد.

## قرارداد ادامه برای عامل بعدی

برای هر قدم فقط این پنج بخش را تولید کن:

1. طرح کوتاه قدم
2. مکث سه‌ثانیه‌ای و اعلام شروع
3. اقدام عملی
4. تست و نتیجهٔ قابل مشاهده
5. قدم بعد یا blocker

قبل از هر تغییر بزرگ، فایل‌های دقیق، منبع حقیقت، معیار پذیرش و مسیر rollback را مشخص کن. اگر دادهٔ واقعی درگیر است، ابتدا read-only check انجام بده.

## منبع حقیقت

- کد: فایل‌های همین workspace
- دادهٔ عملیاتی: PostgreSQL روی VPS
- وضعیت استقرار: systemd سرویس `mehregan.service`
- سلامت عمومی: `/api/health`
- نسخهٔ داده: `/api/data-version`
- برنامهٔ این مرحله: `RM-JSON_STAGE_01_TOKEN_OPTIMIZATION.json`
- لاگ اجرای مرحله: `RM-JSON_STAGE_01_EXECUTION_LOG.json`

## هدف مرحلهٔ جاری

کاهش مصرف توکن و زمان ارتباط بدون حذف تست، امنیت، ثبت پیشینه یا صحت داده. عدد دقیق billing-token در محیط shell قابل مشاهده نیست؛ بنابراین سنجهٔ قابل تکرار فرایندی استفاده می‌شود و محدودیت آن باید صریح بماند.
