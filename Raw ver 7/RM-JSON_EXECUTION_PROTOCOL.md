# قرارداد اجرای مرحله‌ها

این سند مکمل RM-JSON است و برای کاهش متن تکراری و حفظ کیفیت استفاده می‌شود.

## قالب گزارش هر قدم

```text
طرح: [هدف همین قدم در یک یا دو جمله]
مکث: ۳ ثانیه
شروع: [اقدام اکنون آغاز شد]
اقدام: [فقط تغییرات واقعی و کوتاه]
تست: [دستور یا بررسی مشخص]
نتیجه: [موفق/ناموفق + شاهد]
قدم بعد: [شناسهٔ تسک بعدی یا blocker]
```

## قواعد ارتباطی

- یک طرح برای هر قدم؛ طرح دوباره تکرار نشود مگر اینکه دامنه تغییر کرده باشد.
- گزارش‌ها وضعیت، اقدام، تست و نتیجه را نگه دارند؛ توضیحات تزئینی حذف شوند.
- خروجی ابزارها محدود شود؛ credential، token، رمز، محتوای خام پرونده و دادهٔ غیرضروری چاپ نشود.
- تست‌های مستقل در یک اجرای موازی انجام شوند.
- تا وقتی تست لازم انجام نشده، وضعیت `done` اعلام نشود.
- اگر blocker رخ داد، علت، شاهد، راه امن بعدی و تصمیم موردنیاز کاربر کوتاه ثبت شود.

## گیت‌های پذیرش

### گیت A — پیش از تغییر

- فایل‌ها و منبع حقیقت مشخص است.
- دامنهٔ تغییر و مسیر rollback معلوم است.
- دادهٔ واقعی با بررسی read-only محافظت شده است.

### گیت B — پس از تغییر

- lint/type-check یا معادل آن موفق است.
- build موفق است.
- تست اختصاصی همان تغییر اجرا شده است.
- اگر استقرار انجام شده، health، HTTPS و API smoke بررسی شده‌اند.

### گیت C — گزارش

- نتیجه شاهد دارد.
- محدودیت‌ها و سنجه‌های تقریبی با برچسب تخمینی مشخص‌اند.
- artifact یا مسیر فایل ثبت شده است.

## تعریف موفقیت مرحله

مرحله زمانی موفق است که همهٔ تسک‌های بحرانی تست شده باشند، artifact اجرا قابل خواندن باشد، دادهٔ واقعی تغییر ناخواسته نکرده باشد و نتیجهٔ قبل/بعد ثبت شده باشد.
