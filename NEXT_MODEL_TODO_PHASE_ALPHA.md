# TODO مدل بعدی — رساندن پروژه به «تکامل الف»

این سند یک برنامهٔ اجرایی ۱۰‌تسکی است. هر تسک باید با شواهد آزمون بسته شود. اولویت با سلامت جریان اصلی و استقرار است، نه فعال‌کردن امکانات آزمایشی.

## پیش‌شرط‌های ثابت

- مسیر کاری: `Z:\Projects\mehreganClinic\Version5New`
- دادهٔ تولید: PostgreSQL واقعی؛ هرگز دادهٔ نمایشی نساز.
- اسرار: `.env`، `uu.txt`، auth و دادهٔ بالینی را چاپ، commit یا در artifact وارد نکن.
- آزمون پایه: `npm run lint` و `npm run build`؛ برای API، `npm run production:smoke`.
- قبل از deploy: rollback قابل‌بازگشت، سپس بررسی HTTPS و `/api/health`.

## ده تسک اجرایی

### ۱. ممیزی وضعیت و نقشهٔ جریان اصلی

فایل‌های `AGENTS.md`، `server.ts`، `src/App.tsx`، `apiClient.ts` و همهٔ tabهای اصلی را بخوان. برای هر جریان مشخص کن: API واقعی، منبع داده، نقش مجاز، وضعیت UI و آزمون موجود. خروجی: جدول کوتاه در همین سند یا یک گزارش stage.

معیار پذیرش: هیچ قضاوتی بدون ارجاع فایل/endpoint ثبت نشود.

### ۲. تثبیت احراز هویت و نقش‌ها

ورود، خروج، نقش کاربر و نمایش عنوان نقش را با حساب‌های واقعی دیتابیس بررسی کن. عنوان «کارشناس ارشد IT» نباید برای همه ثابت باشد. دعوت کاربر باید توسط مدیر ارشد یا IT انجام شود و نقش در دعوت قفل باشد.

معیار پذیرش: هر نقش فقط مجوز خودش را می‌بیند؛ مسیرهای غیرمجاز ۴۰۳ می‌دهند؛ هیچ رمز hard-code در کد یا commit نیست.

### ۳. تثبیت لایهٔ داده و کش

بررسی کن پاسخ اولیهٔ UI بدون تعلیق طولانی قابل‌استفاده باشد، اما دادهٔ بالینی پس از آماده‌شدن PostgreSQL با fingerprint/version به‌روز شود. کش عمومی سرور و کش محلی کلاینت نباید دادهٔ قدیمی را بی‌تشخیص نمایش دهند.

معیار پذیرش: تغییر version باعث refresh کنترل‌شده شود؛ نبود تغییر باعث درخواست و rerender غیرضروری نشود.

### ۴. پرونده و جستجوی سرپرست/پت

جستجوی چندفیلدی پرونده، سرپرست و پت را با حداقل سه کاراکتر و حداکثر ۱۰ نتیجهٔ مرتبط بررسی کن. null-safe بودن همهٔ فیلدها را تضمین کن. واژهٔ کاربری «مالک/صاحب» در UI باید «سرپرست» باشد.

معیار پذیرش: ورود کاراکتر سوم خطا نمی‌دهد؛ انتخاب نتیجه برای مشاهده و ثبت ورود هر دو کار می‌کند؛ query محدود و قابل‌اندازه‌گیری است.

### ۵. جریان پذیرش و ثبت ورود

از جستجو تا انتخاب پت، ثبت ورود، وضعیت انتظار/معاینه و نمایش پرونده را با دادهٔ واقعی اجرا کن. شمارندهٔ «پرونده‌های موجود در سیستم» نباید با «حاضر در کلینیک» اشتباه شود.

معیار پذیرش: ثبت ورود یک رکورد واقعی می‌سازد و در داشبورد نقش مجاز دیده می‌شود؛ خروج/تغییر وضعیت دوباره‌شماری غلط ایجاد نمی‌کند.

### ۶. واکسیناسیون و پروندهٔ پزشکی

ثبت واکسن، تاریخ، دارو/سری ساخت در صورت وجود، پزشک و یادداشت را بررسی کن. ذخیره باید transactional و قابل‌بازخوانی باشد و در پروندهٔ پت دیده شود.

معیار پذیرش: ایجاد، بازخوانی و خطای اعتبارسنجی آزمون می‌شود؛ دادهٔ ناقص silently موفق اعلام نمی‌شود.

### ۷. نوبت، جراحی و بیهوشی واقعی

جریان نوبت و جراحی را با فرض فعلی کلینیک تطبیق بده: بیهوشی فعلاً وریدی است و گزینهٔ بیهوشی تنفسی نباید فعال یا به‌عنوان امکان موجود نمایش داده شود. حالت force majeure مدیر ارشد باید role-gated و قابل‌ثبت باشد.

معیار پذیرش: گزینهٔ unsupported hidden/disabled است؛ تغییر وضعیت جراحی و داروی بیهوشی در API و پرونده ثبت می‌شود.

### ۸. وظایف، تیکت و اعلان نقش‌محور

ارسال task با اولویت بالا از مدیر ارشد به پرسنل، نمایش در داشبورد گیرنده، تغییر مرحله، و گزارش پیشرفت به فرستنده را کامل کن. تا بسته‌شدن تیکت، سابقهٔ رویدادها نگه‌داری شود.

معیار پذیرش: گیرنده فقط وظایف خودش را می‌بیند؛ مدیر وضعیت را می‌بیند؛ بستن تیکت قابل‌ردیابی است.

### ۹. مخفی‌سازی امکانات ناقص و hardening انتشار

تمام tabها و دکمه‌هایی را که صرفاً mock، local-only، destructive یا بدون API واقعی‌اند فهرست کن. برای کاربر عادی hidden/disabled کن و دسترسی IT را نیز فقط در صورت safe بودن نگه دار. متن‌های نمایشی و URL لوکال را حذف کن.

معیار پذیرش: مسیرهای اصلی بدون دکمهٔ خطادار کار می‌کنند؛ هیچ دادهٔ demo در flow تولید ساخته نمی‌شود؛ lint/build/smoke سبز است.

### ۱۰. آزمون انتهابه‌انتها و استقرار پایدار

یک سناریوی واقعی و کوتاه اجرا کن: ورود پذیرش → جستجوی پت/سرپرست → ثبت ورود → ثبت پرونده/واکسن → مشاهدهٔ پزشک → وظیفهٔ مدیر → خروج. سپس build و deploy با rollback انجام بده.

معیار پذیرش: HTTPS دامنه ۲۰۰، `/api/health` سالم، smoke موفق، سرویس systemd فعال، و نسخهٔ قبلی قابل‌بازگشت است.

## قالب گزارش هر مرحله

```text
تسک: <شماره و عنوان>
نتیجه: <یک یا دو جمله>
آزمون: <نام آزمون و نتیجه>
فایل‌های تغییرکرده: <فقط نام‌ها>
Blocker: <فقط اگر وجود دارد>
قدم بعد: <شمارهٔ تسک بعد>
```

## دستور آغاز برای مدل بعدی

«ابتدا `AGENTS.md`، `WORKSPACE_GUIDE_FOR_NEXT_MODEL.md` و این TODO را بخوان. سپس فقط تسک ۱ را انجام بده: وضعیت را read-only ممیزی کن، بدون تغییر کد، و گزارش را با قالب بالا بده. تا وقتی blockerها و شواهد روشن نیستند سراغ تسک بعد نرو. مسیرهای `node_modules` و داده‌های حساس را اسکن نکن و محتوای secrets را چاپ نکن.»

